pipeline {
  agent any
  stages {
    stage('gitrepo & clean') {
      steps{
        sh "git clone https://github.com/DTN91/Pipeline.git"
        sh "sudo mvn clean"
      }
    }
    stage('Build'){
      steps{
        sh "sudo mvn install -DskipTests=true"
      }
    }
  }
}
